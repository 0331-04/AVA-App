PORT=3002
MONGODB_URI=mongodb://localhost:27017/ava-notifications

# JWT Secret (must be the same value used in the auth and claims modules)
JWT_SECRET=your-shared-jwt-secret

# Firebase - NEVER commit the actual service account JSON file!
# Place your downloaded firebase-service-account.json in src/config/
# See src/config/FIREBASE-SETUP.md for step-by-step instructions
